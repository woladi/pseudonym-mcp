---
'pseudonym-mcp': patch
---

Documentation: the contributor guide described the pre-0.8 rule API
(`pattern`, `engines`, `toPatternDef`), the pipeline diagram still listed the
original five entity types, and the limitations section said dates always reach
the cloud. All three now match what the code does.
